@import 'cozy-ui'

.fil-alerter
  @extends $alerts

  .coz-overlay
    // Specify z-index to bypass upload button z-index
    z-index 59

  .coz-alert
    // Specify z-index to bypass upload button z-index
    z-index 60
