@require 'cozy-ui'

.pho-toolbar
    display flex
    margin-left auto

    a
        &.pho-action-select
            background embedurl("../assets/icons/icon-checkbox.svg") em(16px) center no-repeat
        &.pho-action-rename
            background embedurl("../assets/icons/icon-pen.svg") em(16px) center no-repeat
        &.pho-action-share
            background embedurl("../assets/icons/icon-share.svg") em(16px) center no-repeat
        &.pho-action-delete
            color pomegranate
            background embedurl("../assets/icons/icon-trash-red.svg") em(16px) center no-repeat

    .pho-toolbar-more-btn
            @extends $button--secondary
            @extends $button--more

@media (max-width: (768/basefont)rem)
    .pho-toolbar .pho-toolbar-more-btn
        border        0
        margin-right  0
        height        em(48px, 14px)
        opacity       .5
